# pow 插件

本插件为 [pow](http://pow.cx/)——一个 macOS 上的零配置 Rack 服务器——提供补全和命令。

✅ 启用方式:把「pow」加入 zshrc 的 plugins 数组(可用 omz-pm 一键完成)。

## 命令

- `kapow` 用于重启一个应用。

  ```bash
  kapow myapp
  ```

- `kaput` 用于显示任意 pow 应用的标准输出。
- `repow` 用于重启 pow 进程。
