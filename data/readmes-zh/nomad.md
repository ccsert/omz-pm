# Nomad

`nomad` 插件为 [Nomad](https://nomadproject.io/) 提供简单的自动补全。Nomad 是 Hashicorp 出品的工具,用于以任意规模轻松部署应用。

## 用法

1. ✅ 启用方式:把「nomad」加入 zshrc 的 plugins 数组(可用 omz-pm 一键完成)。

2. 安装 [Nomad](https://nomadproject.io/)

3. 在提示符下输入 `nomad` 并按 `TAB`,即可看到可用的补全选项。
